# Windows 实机验收

## 0.6.0 分类与记录
- 工坊选择 Skins、UI 等总分类及子分类，真实下载次数与来源统计相符，无法取得时不显示为零。
- 文件卡片宽度、间距一致，长文件名换行、字体与单选控件正常。
- 新下载自动归入两层分类，本地目录同步创建；旧新版角色分类不重复。
- 重试失败项后原失败状态消失；单条删除和全部清空后重启，记录不再出现，活动任务与模组文件仍保留。
- 最新上传文件向前 72 小时内的已安装包视作同一版本；旧版本更新列表包含全部范围内文件。
- Windows 11 22H2 及以上实测标题栏云母/亚克力与下方一致，拖动、双击最大化及系统窗口按钮正常。

## 0.5.0 界面与下载
- 未选择有效 GIMI Mods 目录时拒绝下载 Mod，仍可下载官方组件。
- 多个下载顺序排队，等待项可取消；下载期间切换标签、主题及启用其他模组仍可操作。
- 进度、记录和下载错误只在侧栏下载列表显示，重启后中断项可重试。
- 我的模组图片不模糊，开关正常启用/停用，同角色仍互斥。
- 工坊搜索、筛选、翻页、滚动后切换标签，返回时状态不变。
- 浅色、深色和跟随系统设置保存；Windows 11 22H2 以上验证亚克力/云母，其他系统普通背景。
- 使用真实代理下载较大文件，验证断线重试；支持范围请求的同一文件可续传，损坏文件不安装。
- 手动代理保存并测试路由；系统代理模式仍可切回。

## 初次使用
- 完整解压，双击 HoYoMod.exe，中文窗口正常显示；关闭重开后设置保留。
- 下载 XXMI 官方便携组件，校验通过；能打开 XXMI 并完成 GIMI 初始化。
- 识别 GIMI Mods 路径；错误路径有说明，不覆盖原有 Mod。
- 网络不可用时本地库仍能使用，在线页面有重试说明。

## 批量替换 Hash
- 预览时文件不变，列表显示所有命中模组、文件、次数。
- 执行后启用与未启用模组都替换，游戏加载目录中的启用副本同步。
- 重启后替换记录仍在，可按批次回滚，确认原文件恢复。
- 修改文件后再回滚会停止，不覆盖后来的编辑；部署失败后原版本保留。

## 自定义热键
- 安装含多层目录与 Key 配置的模组，打开“查看热键”，确认组合键、反向切换键及来源行号。
- 查看前后 `.ini` 内容一致；旧模组可补扫，更新后热键同步变化。
- 游戏中满足配置条件时验证作者定义的按键；扫描仅展示配置，不执行或推断效果。

## Mod 工作流
- 按角色浏览、搜索、翻页，查看作者预览与文件选项。
- 选择一个兼容当前原神版本的普通外观 Mod，下载、安装、启用。
- 启用同角色另一个 Mod 后，GIMI/Mods/HoYoModManaged 中只有新条目；两个不同角色可以同时存在。
- 从管理器启动原神，确认所选外观生效。
- 运行中切换，确认 F10 刷新可用；窗口无法聚焦时提示手动 F10。
- 保存组合、全部停用、应用组合，验证游戏与本地部署一致。

## 更新、错误和便携
- 默认下载不启用；开启自动启用后下载即切换。
- 自动检查只提示；手动安装更新后保留启用状态和方案引用。
- 切换三种排序和 SFW/NSFW 勾选；NSFW 图片默认模糊，点击显示，设置开关生效。
- 下载记录保留网址与文件上传日期；安装失败后查看原因并重试缓存。
- 检查结束汇总全部更新、失败及未知项；最新 72 小时内的文件全部列出，不自动选择。
- 遇到多文件更新明确选择；断网/损坏包后旧版本仍可使用。
- 含脚本或路径穿越的包拒绝安装。
- 关闭软件，移动完整便携目录后重开，库正常；内部组件与外部路径按界面提示重新设置。

上述涉及 Windows 游戏进程的项目在当前 macOS 环境无法替代实机验证，不标记为已通过。
