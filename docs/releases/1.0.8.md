# HoYoMod 1.0.8

日期：2026-09-24。状态：已发布，待 Windows 实机验收。

源码：`e484383`（`v1.0.8` 标签与主线合并提交）；功能提交 `911bc40`（通知中心交互、版本号与验收项），此前整合提交 `acee4b0`（上一轮工坊与游戏菜单修复）。

变更与兼容性：Mod 详情及其大图预览打开期间，右下角通知中心仍可操作；关闭弹窗后恢复原位置。整合单文件下载次数、问号样式、图片切换和游戏右键菜单修复。用户 `data`、安装库、启用库与 GIMI/ZZMI/SRMI/WWMI 不迁移。

执行环境：macOS，Node.js 24.21.0，pnpm 11.19.0，Electron 44.3.0；本地构建 Windows x64 便携 ZIP。

已执行检查：

- `pnpm install --frozen-lockfile`：通过，锁文件未变化。
- `pnpm check`：446 项中 444 通过、0 失败、2 项按平台跳过。
- `node --test tests/ui-assets.test.cjs`：5 项通过；界面资源禁止缓存、资源白名单及启动清缓存规则保持有效。
- `node scripts/smoke-notification-layer-and-pager.cjs`：详情打开后收到通知，通知中心可展开、收起；关闭详情后仍可使用，位置不变；工坊翻页回顶通过。
- `node scripts/smoke-notifications.cjs`：通知展示、删除、自动收起和重启恢复通过。
- `pnpm exec electron scripts/smoke-workshop-comments.cjs`：评论、单文件下载数、大图切换及返回、文件直达下载通过。
- `node scripts/smoke-multi-game.cjs`：四游戏隔离、侧栏游戏右键菜单、大图预览及重启恢复通过。
- `pnpm pack:win`：成功；`ELECTRON_RUN_AS_NODE=1 pnpm exec electron scripts/verify-windows-package.cjs`：包内源码、Windows x64 PE、归档工具及资源通过；`ELECTRON_RUN_AS_NODE=1 pnpm exec electron scripts/smoke-update-package.cjs`：真实 ZIP 更新准备、配置和模组保护及恢复检查通过。
- [GitHub CI 运行 35989741526](https://github.com/zhr-666/HMM-HoyoModManager/actions/runs/35989741526)：macOS 与 Windows 检查通过；Windows x64 构建、包校验、更新准备和桌面导航冒烟测试通过。

Windows 实机待验收：从 v1.0.7 就地升级后确认详情页通知中心可操作且界面资源为最新版；核对 data、加载器 Mods、图标、首次启动、更新失败恢复及真实游戏行为。macOS 构建和模拟检查不能替代这些结果。

产物：`HoYoMod-1.0.8-Windows-x64.zip`，182986164 字节，SHA256 `441069b8550cdd8e28711dbf302dd501c56903cc822aadfe40baa0d60a7f1f0c`。

发布核对：远端标签指向 `e484383`，Release 正文为空，文件名、字节数及 GitHub 报告的 SHA256 与本地一致。链接：<https://github.com/zhr-666/HMM-HoyoModManager/releases/tag/v1.0.8>。

更新失败时的恢复方法：退出 HoYoMod，将 v1.0.8 ZIP 完整解压到原程序目录并覆盖程序文件，保留 `data`、GIMI/ZZMI/SRMI/WWMI、Mods 和个人文件；若自动更新留下恢复提示，按程序目录内的恢复脚本与说明处理。

发布授权：用户明确要求发布 v1.0.8 到 GitHub。未提供 Release 正文，按项目规则留空。
