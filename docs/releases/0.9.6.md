# HoYoMod 0.9.6

日期：2026-09-17。状态：待验收（未发布、未创建标签）。源码标识：分支 `codex/fix-update-helper-lifetime`，修复提交 `bef0fa2`，随本记录一并提交。

## 变更

- 修复下载完成后点击“重启并更新”，HoYoMod 关闭后没有替换程序文件、也没有启动新版的问题。
- 原因：助手以 `detached:false` 启动，Windows 上会被并入带 `KILL_ON_JOB_CLOSE` 的 job object，主程序退出时助手被一起结束；`unref()` 只释放事件循环引用，不解除该绑定。
- 助手改为独立进程启动，HoYoMod 退出后继续完成替换并启动新版。日志、隐藏窗口、就绪握手、启动失败诊断与超时清理不变。
- 兼容性：替换范围与保护路径不变，仍只替换程序运行文件，保留整个 `data`、启用组合、名称、搭配方案、GIMI 路径、外部程序和自定义背景。0.9.5 及更早版本遇到该问题时，仍须手动覆盖一次，旧助手无法通过失败过的自动更新流程修复自身。

## 验证

- macOS，Node.js v24.21.0，pnpm 11.19.0（本机未预装，安装到临时目录后用于构建）。
- `node scripts/check-syntax.cjs`：56 个 JavaScript 文件语法检查通过。
- `node --test tests/*.test.cjs`：149 项，147 通过、0 失败、2 跳过（两个 Windows 专属用例在本机跳过）。`pnpm check` 本身未按原命令跑通：本机 pnpm 会在运行脚本前执行依赖校验并触发 `pnpm install`，该步骤在受限网络下失败，因此改用等价底层命令并如实记录。
- 断言变异验证：把 `src/core/update-helper.cjs` 临时改回 `detached:false`，`tests/update-helper.test.cjs` 对应用例立即失败；还原后全部通过，源文件与 `bef0fa2` 一致。
- `electron-builder --win zip --x64`：构建成功。构建读取 pnpm store，依赖收集器在本次环境中回退为 npm。
- `ELECTRON_RUN_AS_NODE=1 electron scripts/verify-windows-package.cjs`：通过。Windows x64 PE、包内源码与 package.json 版本一致、7za.exe、unrar.wasm、RAR 解压均校验通过。
- `HOYOMOD_TEST_PACKAGED=1` 下 `scripts/smoke-update-package.cjs`：通过。真实 ZIP 的准备流程、物理 ASAR、包内助手可读、配置与模组保留、目录冒充被拒均校验通过。
- ZIP 条目 115 条，与 0.9.5 的文件清单逐条比对完全一致，确认依赖收集器回退未改变包内容。
- Windows 实机结果：未执行。

## 产物

- 文件：`HoYoMod-0.9.6-Windows-x64.zip`
- 字节数：159043081
- SHA256：`7977b9f602bd57b19dd58469e03cd9f2b4cb47dc5bc4c21cb4b713812a15a4f3`
- 发布地址：未发布。

## 未验证项与已知问题

- Windows 上助手在 HoYoMod 退出后的实际替换与重启、游戏内模组加载均未验收。
- 两个 Windows 自动化用例（真实 Electron 父进程退出后助手存活、完整 handoff 替换并启动新版）在本机为跳过状态，尚无实际执行证据；需要 Windows CI 或 Windows 实机运行。
- “修复前该用例确实失败”这一复现结论同样只有 Windows 环境才能取得，本记录不将其标成已确认。
- 本轮改动未推送，未创建标签或 Release。

## 更新失败时的恢复方法

旧版如果更新助手启动失败，请退出 HoYoMod，将完整 ZIP 解压覆盖到原程序目录，保留原 data、GIMI、Mods 及个人文件，不删除旧目录、不只替换 EXE。已经开始替换但未完成的更新，应先关闭 HoYoMod 并使用原更新目录对应的 `HoYoMod-Recover.cmd` 恢复，同时保留 `.hoyo-updates` 中的日志和备份。
