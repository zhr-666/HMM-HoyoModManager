# HoYoMod 1.0.5

日期：2026-09-24。状态：待 Windows 实机验收。

源码提交：`901b453`（鸣潮与按需游戏工作空间）、`accbb51`（空首页、游戏卡片跳转、库洛官方背景和版本号）。悬浮窗与 OCR 的未提交工作未进入本版。

变更与兼容性：支持鸣潮、WWMI 和 GameBanana 模组管理；首次启动不预建游戏数据，用户从「全部游戏」添加后才创建工作空间。空首页显示灰色背景；单击游戏卡片进入该游戏首页，双击仍进入工作空间。鸣潮可从库洛官方启动器获取当前背景图。已有 `data` 和手动模组保持原有位置；用户确认移除游戏时会删除该游戏数据及本程序管理的启用模组。

执行环境：macOS，Node.js 24.21.0，pnpm 11.19.0，Electron 44.3.0；构建 Windows x64 便携 ZIP。

检查命令与结果：

- `pnpm check`：417 项测试中 415 通过、0 失败、2 项 Windows 专用测试按平台跳过。
- `node --test tests/ui-assets.test.cjs tests/kuro-background.test.cjs tests/network.test.cjs`：18 项通过；库洛当前接口返回的背景图片已实际下载并核对 WebP 文件头。
- `node scripts/smoke-multi-game.cjs`：真实 Electron 界面通过空首页、游戏添加后跳转、双击工作空间、四游戏切换、数据隔离和移除恢复检查；已检查空首页截图。
- `pnpm pack:win`：成功；`ELECTRON_RUN_AS_NODE=1 pnpm exec electron scripts/verify-windows-package.cjs`：Windows x64 程序与包内源码校验通过；`ELECTRON_RUN_AS_NODE=1 pnpm exec electron scripts/smoke-update-package.cjs`：真实 ZIP 的更新准备、保护数据和恢复检查通过。
- `git diff --check`：通过。本独立工作区运行 `pnpm start` 产生的忽略目录 `data` 已清理；主工作区的悬浮窗改动未修改。

Windows 实机结果：待验收。需从 v1.0.3 升级并按 `docs/acceptance.md` 核对新版界面缓存、首次启动空首页、鸣潮 WWMI、获取官方背景、右键移除游戏、更新失败恢复和程序图标。

产物：`HoYoMod-1.0.5-Windows-x64.zip`，163235656 字节，SHA256 `8c873a8da8f3eafe87a957c2c249bfdb51a8e1dc5fba915e65e146f8448f2092`。

未验证项：Windows 桌面交互和实际升级重启、Windows 上的 WWMI 游戏内效果。macOS 冒烟及 Windows 包检查不能替代这些验收。

更新失败时的恢复方法：退出 HoYoMod，将 v1.0.5 ZIP 完整解压到原程序目录并覆盖程序文件，保留原 `data`、GIMI/ZZMI/SRMI/WWMI、Mods 和个人文件；若自动更新留下恢复提示，按程序目录内的恢复脚本与说明处理。

发布授权：用户明确要求发布 GitHub v1.0.5，Release 正文为“优化动画逻辑  支持wuwa模组管理。”。发布链接待填写。
