# HoYoMod 1.0.6

日期：2026-09-24。状态：待 Windows 实机验收。

源码提交：`84c582b`（版本号）；功能合并提交 `a911eaa`。

变更与兼容性：增添首次使用教程，优化鸣潮背景显示，优化部分逻辑问题。包含原神快捷键浮层与游戏侧栏图标排序。保留用户的 `data`、加载器目录和手动模组；本版不做数据迁移。

Release 正文（用户提供）：增添了教程，优化鸣潮背景显示，优化部分逻辑问题

执行环境：macOS，Node.js 24.21.0，pnpm 11.19.0，Electron 44.3.0；本地构建 Windows x64 便携 ZIP。

检查命令与结果：

- `pnpm install --frozen-lockfile`：通过，锁文件未变化。
- `pnpm check`：437 项测试中 435 通过、0 失败、2 项按平台跳过。
- `node scripts/smoke-multi-game.cjs`：四游戏资源、侧栏排序、数据隔离与重启恢复通过。
- `node scripts/smoke-onboarding-guide.cjs`：首次使用、五步教程、关闭和重放通过。
- `pnpm exec electron scripts/smoke-game-hotkey-overlay.cjs`：快捷键浮层通过。
- `pnpm pack:win`：成功；`ELECTRON_RUN_AS_NODE=1 pnpm exec electron scripts/verify-windows-package.cjs`：Windows x64 程序和包内源码通过；`ELECTRON_RUN_AS_NODE=1 pnpm exec electron scripts/smoke-update-package.cjs`：ZIP 更新准备、数据保护和恢复检查通过。
- [GitHub Actions 运行 35951666798](https://github.com/zhr-666/HMM-HoyoModManager/actions/runs/35951666798)：Windows 与 macOS 检查通过；Windows 任务完成 x64 ZIP 构建、包内容与更新准备校验，并在桌面通过游戏导航冒烟测试。

Windows 实机结果：CI 的 Windows 桌面导航和更新准备检查通过。仍待在装有游戏及加载器的 Windows 机器上从 v1.0.5 升级，确认教程、鸣潮背景、界面缓存、快捷键浮层、更新失败恢复和程序图标。

产物：`HoYoMod-1.0.6-Windows-x64.zip`，183035750 字节，SHA256 `f055ab566180680da1aef1a9354ba3fcacbea39552bc866bf06dc6d9607953d9`。

未验证项：Windows 真机从旧版升级后的界面资源刷新，以及真实游戏和加载器中的行为。

更新失败时的恢复方法：退出 HoYoMod，将 v1.0.6 ZIP 完整解压到原程序目录并覆盖程序文件，保留原 `data`、GIMI/ZZMI/SRMI/WWMI、Mods 和个人文件；若自动更新留下恢复提示，按程序目录内的恢复脚本与说明处理。

发布授权：用户明确要求发布到 GitHub，并提供上述 Release 正文。发布链接待填写。
