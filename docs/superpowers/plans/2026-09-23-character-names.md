# 角色分类名汉化实施步骤

1. 核对三个游戏当前的 GameBanana 角色分类 ID，与官方简体中文名称配对；未确认名称不加入映射。
2. 新增本地 ID 映射与只读安装记录投影，在 GameBanana 分类、列表、详情及旧分类缓存回退路径应用映射。
3. 增加三游戏回归测试，覆盖未知分类、UI、旧安装记录、路径和标题不变。
4. 更新维护说明，运行专项测试、`pnpm check` 和差异检查；不升版、不打包、不发布。
