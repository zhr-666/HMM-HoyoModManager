# 启动时按游戏加载状态

1. 先写工作区回归测试：非当前坏状态不阻止启动、切换失败不改变当前游戏、修复后可重试。
2. 调整 `Workspaces` 的初始化顺序和按需初始化，保留测试/工具显式全量初始化能力。
3. 主进程按需创建目标游戏运行服务，启动后台任务只覆盖已加载游戏；软件更新操作前获取所有游戏的路径保护。
4. 运行 `pnpm check`、界面资源缓存测试和多游戏界面冒烟，审查差异并记录 Windows 未验证项。
