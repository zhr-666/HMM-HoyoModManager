# 项目开发流程落地计划

## 目标与范围

用户授权由助手决定本项目的规范化方式。采用适合个人维护的需求澄清、实现、验证、审查、提交、发布流程。本次只增加项目规则、开发文档、基础检查命令与 CI 配置；保留工作区已有的 0.9.4 更新器改动。当前工作区直接落地，不提交混合改动，不自动上传。

## 实施步骤

- [x] 增加根目录 AGENTS.md：长期协作偏好、变更边界、验证要求、发布授权。
- [x] 增加 docs/development.md：环境、检查命令、测试分层、分支提交、发布清单和验证记录模板。
- [x] 增加 scripts/check-syntax.cjs，遍历 src、scripts、tests 的 JavaScript 文件，用当前 Node 执行语法检查，失败返回非零。
- [x] 在 package.json 增加 check:syntax、check 入口，固定开发工具版本；文档提供 Electron Node 模式的产物检查命令，保留已有字段。
- [x] 增加跨平台 Windows/macOS CI，仅安装锁文件依赖、运行基础检查，不上传或发布。
- [x] 增加 PR 模板与 README 开发入口。
- [x] 运行基础检查，用临时非法 JavaScript 文件确认检查能失败，清理临时文件；检查差异，记录本次结果及未验证项。

## 验收

统一入口实际运行通过；非法语法返回失败；未触碰已有业务代码修改；CI 仅配置完成，本地结果不能声称远程 CI 或 Windows 实机通过。


## 验证记录（2026-09-16）

- 环境：macOS，Node.js 24.16.0，pnpm 11.19.0。
- `pnpm check`：54 个 JavaScript 文件语法检查通过；137 项测试通过，0 失败、0 跳过。
- 失败路径：在 scripts 下创建临时非法 JavaScript，检查返回退出码 1 并指出文件；随后清理该临时目录。
- `pnpm install --lockfile-only --offline --ignore-scripts --frozen-lockfile`：通过，锁文件无变化。这不是全新环境安装验证。
- CI YAML 本地解析通过，新增指南和规则中的相对文档链接均存在。
- `git diff --check`：通过。
- 自审：已有 0.9.4 版本号和更新器修改保留，本次新增规则、文档和基础检查配置；未提交、未推送、未构建或发布。
- 限制：远程 CI、桌面界面测试与 Windows 实机验收本次未运行；Playwright 尚未锁定到项目依赖，指南已明确环境要求与后续完善项。
