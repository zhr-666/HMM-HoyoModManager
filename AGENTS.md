# HoYoMod 项目协作规则

## 用户协作偏好

- 需求有歧义或存在实质设计取舍时，先澄清，每次只问一个问题。
- 对目标达到约 95% 理解后，说明方案并直接执行，不重复索要确认。
- 简单明确的请求直接完成；用户授权代为决定时，采用适合当前项目规模的方案并解释原因。

## 每次开发

1. 阅读 README.md、docs/development.md 和相关模块；先检查 git status 与差异，识别既有未提交工作。
2. 明确需求、受影响行为和验收条件。小改动在对话中说明；跨模块或涉及数据迁移、更新恢复的改动写入 docs/superpowers/specs 与 plans。
3. 保留不属于本任务的修改。不得通过 reset、覆盖或清理未跟踪文件来获得干净工作区。按任务选择 feat/ 前缀分支；已有改动需先确认归属，不能混入其他任务提交。
4. 修复缺陷先复现并定位原因；业务行为变化补充有意义的回归测试，覆盖失败和恢复路径。纯文档修改检查文档与链接，不强行编写测试。
5. 按 docs/development.md 运行必要检查，审查差异。报告实际命令、结果与未验证项，不能用历史测试记录替代本次结果。
6. 提交只包含本任务文件，使用明确路径暂存并检查暂存差异。未提交状态可交付，但必须明确说明。

## 项目约束

- 术语固定：**安装**指把下载或本地选择的压缩包解压后的模组副本放进程序目录下的 `data\games\<游戏 ID>\library`（界面「安装库」），只决定模组在本机库里的分类归属、不写 GIMI；**启用**指把安装库里的模组副本放进 GIMI 文件夹（`GIMI\Mods\HoYoModManaged\<模组 ID>` 或所选 Mods 子目录，界面「启用库」），是游戏内真正生效的一步。界面文案、文档与新增代码描述这两步时统一用这两个词。
- 界面资源的缓存规则（每次更新都必须守住）：更新只替换程序文件、保留 `data`（含 Chromium 的会话缓存），所以界面资源一旦被缓存，就会出现「代码改了、界面还是旧的」。因此 ①`hoyo://` 下发的每个界面文件都必须带 `cache-control: no-store`；②每次启动必须清掉 `data/session` 下的 Chromium 缓存目录；③新增界面文件要同时进 `src/core/ui-assets.cjs` 的 `UI_ASSETS` 白名单。三者由 `tests/ui-assets.test.cjs` 守住，改界面后必须重跑该测试；发布前在 Windows 上确认更新后界面确实是最新版。
- 保持 Windows x64 便携程序定位；macOS 开发结果不能替代 Windows 实机验收。
- 保护用户 data、GIMI、手动模组及个人文件；文件操作需考虑失败、取消和恢复。
- 模组内程序与脚本仅安装，不执行。保留现有外部 EXE 由用户明确选择和点击启动的边界。
- 引入依赖时说明用途，同步 pnpm-lock.yaml。不得把本机绝对路径写入共享配置。
- 不为流程整理大规模重排业务代码或批量格式化历史文件。
- 软件版本号由用户决定。未获得用户指定的版本号时，不自行修改 `package.json` 版本或制作新的带版本号产物；日常文档和工具改动也不自动升版。可以按改动性质向用户建议版本号，但不得代用户决定。同一个版本号只对应一份产物，改动后不能用旧版本号重新出包。
- 仅当用户明确要求“上传”或“发布”时才推送发布产物、创建正式 Release。构建、测试和本地完成均不构成发布授权。

完整流程与发布清单见 [开发指南](docs/development.md)。

## 仓库与版本说明

- 当前项目仓库与软件更新源统一为 `zhr-666/HMM-HoyoModManager`。用户要求新项目从 v1.0.0 重新开始，后续版本号由用户指定。
- README 仅保留简短的项目总介绍、使用方式和开发入口，不追加逐版本更新记录。
- GitHub Release 的版本说明由用户亲自填写；除非用户提供正文或明确另行要求，否则创建 Release 时正文留空，不自动生成更新说明。
